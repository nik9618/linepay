// 'use strict';

// var SDK = require('inline-sdk');
// var CommandType = SDK.CommandType;
// var inline = new SDK();

// inline.onCommand((type, payload, context) => {
//   if (type !== CommandType.MESSAGE) return;

//   // context.sendText(payload.args.join(' '));
//   // console.log(payload.args.length);

// });

